services:
  redis:
    image: redis:7.2.4
    container_name: airline-redis

    ports:
      - "6379:6379"

    volumes:
      - redis_data:/data

    command: redis-server --appendonly yes

volumes:
  redis_data: